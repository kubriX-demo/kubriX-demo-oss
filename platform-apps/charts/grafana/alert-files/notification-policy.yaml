apiVersion: 1
policies:
    - orgId: 1
      receiver: grafana-default-email
      group_by:
        - grafana_folder
        - alertname
      routes:
        - receiver: Vision22 (Teams)
          object_matchers:
            - - grafana_folder
              - =
              - kubernetes-apps